using Climb.Data;

namespace Climb.ViewModels.User
{
    public class HomeViewModel : BaseViewModel
    {
        public HomeViewModel(ApplicationUser user)
            : base(user)
        {
        }
    }
}